# Copy to .env and edit for local testing. Do NOT commit .env with secrets.
SMTP_HOST=localhost
SMTP_PORT=1025
FROM_ADDR=security-sim@example.local
BASE_URL=http://127.0.0.1:5000
SIM_DB_PATH=sim.db
EXPORT_DIR=exports
